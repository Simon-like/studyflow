import { colors, alpha, spacing } from '../../../theme';

export const HEADER_HEIGHT = 56;
export const HEADER_BACKGROUND = `${colors.surface}CC`; // 80% 透明度
export const HEADER_BORDER_COLOR = alpha.mist30;
export const HEADER_PADDING = spacing.md;
